// Export the default export from the 'Navbar' module as 'Navbar'
export { default as Navbar } from './Navbar';

// Export the default export from the 'main' module as 'Main'
export { default as Main } from './main';

// Export the default export from the 'Products' module as 'Product'
export { default as Product } from './Products';

// Export the default export from the 'Footer' module as 'Footer'
export { default as Footer } from './Footer';
