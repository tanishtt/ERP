// Exporting the Navbar component from the './Navbar' file
export { default as Navbar } from './Navbar';

// Exporting the Main component from the './main' file
export { default as Main } from './main';

// Exporting the Product component from the './Products' file
export { default as Product } from './Products';

// Exporting the Footer component from the './Footer' file
export { default as Footer } from './Footer';
