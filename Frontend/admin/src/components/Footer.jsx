import React from 'react'; // Importing React to use JSX and React components

// Defining the Footer functional component
const Footer = () => (
  <div className="mt-24">
    {/* Main container for the footer with top margin */}
    <p className="dark:text-gray-200 text-gray-700 text-center m-20">
      {/* Paragraph for the footer text, with conditional styling for dark mode */}
    </p>
  </div>
);

export default Footer; // Exporting the Footer component as the default export
